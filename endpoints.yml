action_endpoint:
  url: http://localhost:5055/webhook

A2A:
  farmarag_agent:
    url: http://localhost:8000
    call_endpoint: /ask
    health_endpoint: /

model_groups:
  - id: gemini_llm
    models:
      - provider: gemini
        model: gemini-2.5-flash
        api_key: ${GEMINI_API_KEY}
